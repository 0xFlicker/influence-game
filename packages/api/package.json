{
  "name": "@influence/api",
  "version": "0.6.0",
  "description": "Influence game HTTP API — Bun + Hono server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "test:mock": "echo 'no tests yet'"
  },
  "dependencies": {
    "@influence/engine": "workspace:*",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "bun-types": "^1.3.10",
    "typescript": "^5.8.3"
  }
}
