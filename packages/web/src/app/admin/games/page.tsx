import { AdminPageShell } from "../admin-page-shell";

export const metadata = {
  title: "Games — Influence Admin",
};

export default function AdminGamesPage() {
  return <AdminPageShell activeTab="games" />;
}
